<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/main_items"
        android:checkableBehavior="single"
    >
        <item
            android:id="@+id/nav_all_queues"
            android:icon="@android:drawable/ic_menu_camera"
            android:title="@string/drawer_item_all_queues"
        />
        <item
            android:id="@+id/nav_my_queues"
            android:icon="@android:drawable/ic_dialog_dialer"
            android:title="@string/drawer_item_my_queues"
        />
    </group>

    <group
        android:id="@+id/additional_items"
        android:checkableBehavior="none"
    >
        <item
            android:id="@+id/nav_settings"
            android:icon="@android:drawable/ic_menu_share"
            android:title="Настройки"
        />
        <item
            android:id="@+id/nav_help"
            android:icon="@android:drawable/ic_menu_share"
            android:title="Справка"
        />
    </group>

</menu>